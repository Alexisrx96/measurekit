# measurekit/dimensions.py
"""Este módulo expone las instancias de Dimensiones base del sistema.

Las dimensiones se cargan dinámicamente durante la inicialización
desde el archivo de configuración de MeasureKit.

Ejemplo de uso:
from measurekit import dimensions

fuerza = dimensions.FORCE
"""

# Este módulo se llenará con atributos (las dimensiones)
# de forma programática en el módulo measurekit.startup.py.
